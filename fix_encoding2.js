const fs = require('fs');
let html = fs.readFileSync('public/admin.html', 'utf8');

const target1 = '<p class="text-[11px] font-bold text-slate-500">??? ?? ???? ?????? ??????? ??????. ????? ?????? ?? ??? ??? ????? ????? ????????.</p>';
const target1Fix = '<p class="text-[11px] font-bold text-slate-500">??? ?? ???? ?????? ??????? ??????. ????? ?????? ?? ??? ??? ????? ????? ????????.</p>';

html = html.replace(target1, target1Fix);

// Find the label
const idx = html.indexOf('<label class="text-sm font-bold text-slate-900/70 block mb-1">');
if(idx !== -1) {
  const endIdx = html.indexOf('</label>', idx);
  const oldLabel = html.substring(idx, endIdx + 8);
  const newLabel = '<label class="text-sm font-bold text-slate-900/70 block mb-1">????? ?? ??? ??? ?????? (?????? ?????? ????? ??????)</label>';
  html = html.replace(oldLabel, newLabel);
}

fs.writeFileSync('public/admin.html', html, 'utf8');
console.log('Fixed admin.html');
