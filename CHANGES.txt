تعديلات على النسخة المرسلة (eco_v2_final_clean) - 5 ملفات فقط
ضع الملفات في نفس مساراتها (تستبدل الموجودة).

1) routes/settings.js
   - أسطر 66 و102: أُعيدت الأقواس في شرط (user_id = $1 OR user_id IS NULL) AND status ...
     كان المخزون يتضخم عند تصفير الإحصائيات/إعادة تعيين المتجر (5 ← 11 بدل 7).
   - سطرا الروابط الاجتماعية (GET/PUT /social): user_id = $1 AND key = $2
     كانت الروابط تنهار في صف واحد بعد "إعادة تعيين المتجر".
   - إعادة تعيين المتجر لم تعد تحذف صفوف الإعدادات العامة (user_id IS NULL).

2) public/js/admin.js
   - تبويب الروابط يمرّر store_id عند القراءة (لأن الحفظ صار خاصاً بالمالك).
   - زر "تعديل" الإنفاق الإعلاني: استُبدل onclick المضمَّن (تحجبه سياسة CSP في helmet)
     بمستمع + data-attribute. كما يُعرض التاريخ بصيغة YYYY-MM-DD.

3) routes/campaigns.js
   - إضافة PUT /api/campaigns/spend/:id (كان العميل يستدعيه دون وجوده في الخادم).

4) public/product.html
   - إزالة كتلة "if (product && ...)" العامة (كانت ترمي ReferenceError عند كل تحميل)،
     واستدعاء initSocialIcons بعد تحميل المنتج. تظهر الآن أيقونات التواصل في تذييل الصفحة.

5) .gitignore
   - استثناء eco-store.sqlite* حتى لا تُرفع قاعدة بياناتك الحقيقية مع الكود.
